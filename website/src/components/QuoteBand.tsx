export function QuoteBand() {
    return (
        <section id="why">
            <div className="wrap">
                <div className="quote-band reveal">
                    <span className="mark" aria-hidden="true">
                        &ldquo;
                    </span>
                    <blockquote>
                        Your agent re-learns everything every session. You explained your deploy flow last week; today it
                        has <b>no idea.</b>
                    </blockquote>
                    <p className="who">
                        A memory layer is a deliberately written, persistent store that is the source of truth. You teach
                        your agent something once — and it remembers forever, across every repo and every session. gemdex
                        gives it durable memory you write on purpose: once, recallable everywhere.
                    </p>
                </div>
            </div>
        </section>
    );
}
